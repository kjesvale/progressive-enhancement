{
    "name": "progressive-enhancement",
    "version": "1.0.0",
    "scripts": {
        "start": "parcel src/*.html",
        "build": "parcel build src/*.html --dist-dir server/public"
    },
    "devDependencies": {
        "nodemon": "^2.0.15",
        "parcel": "^2.3.1",
        "prettier": "^2.5.1",
        "ts-node": "^10.5.0",
        "typescript": "^4.5.5"
    },
    "prettier": {
        "tabWidth": 4
    }
}
